# ZKP_lite
This is a quick insight in my bachelor degree's project. 
As it is only a lite version, I didn't add the build of the circuit as well as the proofs of the "handshake" (if tokens are matching which in lite version are, therefore the public proofs should be true as well. If not, there might be version clashing of languages used.
